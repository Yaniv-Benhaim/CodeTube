<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="220dp"
    android:layout_height="220dp"
    android:clickable="true"
    android:layout_marginLeft="8dp"
    android:focusable="true"
    >

    <androidx.cardview.widget.CardView
        android:id="@+id/item_course_img_cardview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="16dp"
        app:cardCornerRadius="10dp"
        app:cardElevation="5dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent">
    <ImageView

        android:scaleType="centerCrop"
        android:id="@+id/item_course_img"
        android:layout_width="220dp"
        android:layout_height="130dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_launcher_background" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/item_course_title"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:text="Full Android studio course - Beginner to Advanced"
        android:textColor="#000"
        android:maxLines="2"
        android:ellipsize="end"
        android:textSize="16sp"
        android:textStyle="normal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/item_course_img_cardview"
        />
    <ImageView
        android:id="@+id/rating_course_rv"
        android:layout_width="100dp"
        android:layout_marginTop="75dp"
        android:layout_height="wrap_content"
        android:src="@drawable/rating_img"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>




</androidx.constraintlayout.widget.ConstraintLayout>